import React from "react";

const Button = () => {
  return (
    <button className="inline-flex py-4 px-14 justify-center items-center gap-2 rounded-md border border-black border-solid bg-white text-black">
      Button
    </button>
  );
};

export default Button;
